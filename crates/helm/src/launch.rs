//! Cask launch path: verify -> build CSpace -> map -> start.
//!
//! TODO(helm): call `cask::verify`, intersect license/manifest/delegated
//! capabilities, create the VSpace, and hand the task to Keel.
//! Refuse launch on any verification failure (fail-closed).
pub fn launch_cask() { todo!("verify + launch a Cask") }
